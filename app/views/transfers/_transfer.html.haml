= form_for transfer, target: :_top do |f|
  = f.label :seller
  = f.text_field :seller, readonly: true, value: current_user.id
  = f.label :player
  = f.collection_select :player, current_user.players, :id, :brief_info
  = f.label :price
  = f.number_field :price
  = f.submit 'Add A New Transfer'
